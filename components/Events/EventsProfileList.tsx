import { SmallCardEvent } from "./SmallCardEvent";

export const EventsProfileList = () => {
    return (
        <section className="flex justify-center gap-4 flex-wrap">
            <SmallCardEvent />
            <SmallCardEvent />
            <SmallCardEvent />
            <SmallCardEvent />
            <SmallCardEvent />
        </section>
    )
}
